# 管理员密码（用于后台 /admin 登录）
# 生产环境务必修改为强密码
ADMIN_PASSWORD=change-me

# 登录 Cookie 签名密钥，建议使用 32 位以上随机字符串
AUTH_SECRET=replace-with-a-long-random-secret

# Neon 数据库连接
# 本地开发不设置时，会自动使用 data/*.json 作为数据存储
DATABASE_URL=postgres://...
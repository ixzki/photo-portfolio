# 后台足迹 CSV 导入

原始 CSV 在浏览器的独立 Web Worker 中逐块读取，仅保留选定时间范围内的轨迹。原始文件不会上传到 Vercel 或写入 Neon；保存旅行时提交筛选后的路线及编辑内容。

## 字段与格式

| 内容 | 支持的表头示例 |
| --- | --- |
| 时间 | `dataTime`、`timestamp`、`time`、`datetime`、`date`、`record_time`、时间、记录时间、定位时间 |
| 海拔（可选，米） | `altitude`、`alt`、`elevation`、`ele`、`height`、海拔、海拔高度、高程 |
| 纬度 | `latitude`、`lat`、纬度 |
| 经度 | `longitude`、`lng`、`lon`、经度 |
| 定位误差（米，可选） | `accuracy`、`horizontal_accuracy`、精度、水平精度、定位精度 |

表头不区分大小写，忽略空格、下划线和连字符。同一类字段若出现多列会提示先保留一列，不会猜测使用哪列。

- CSV 支持 UTF-8、带 BOM 的 UTF-16 LE/BE；支持逗号分隔、双引号转义、引号内换行和 Windows 换行。其他编码请先另存为 UTF-8 CSV。
- 坐标使用 **WGS84**，纬度范围 ±85.051129、经度范围 ±180。导入器不进行 GCJ-02/BD-09 坐标转换。
- 时间支持 Unix 秒、Unix 毫秒及 `2026-06-24T08:00:00+08:00` 等 ISO 格式。没有时区的时间固定按北京时间 UTC+8 处理，不受电脑时区影响。
- 开始和结束时刻都包含在筛选范围内；例如结束 `10:25` 表示截至 `10:25:00`。如需完整一天，使用下一天开始前的 `23:59:59`。
- 定位误差设为 0 时不筛选；启用时必须有精度列，超过设定误差的点会跳过。
- 轨迹按时间排序，相邻位置重复时去重。默认相隔超过 60 分钟或 5 公里即断段，不连接缺失轨迹；不足两个点的孤立段不生成线路。
- 时间和可选海拔随每个保留点一起保存，去重、断段时保持对应。缺失或无效海拔用空值，不用 0 代替；实际记录的 0 米保留。
- 输出最多 80,000 个点、3 MB，超出后要求缩小日期范围，不会自动抽稀改变路线。导入时不会识别或自动移除航班，应在时间范围中选定自驾起止时刻。

导入结果展示 CSV 记录总数、筛选后记录数、最终点数、线路段数和无效记录数。无效时间、坐标、选定时段内的无效精度或列数错误会计入无效记录；破损的 CSV 引号结构会停止导入并提示修复文件。

## 新建与补齐记录

读取后同时预览已记录公里数、行程时长和实际记录起止时间，使用与详情页封面相同的计算函数。公里数只累加连续轨迹段，时长包含停留和过夜；表单的筛选时间可能宽于实际记录，封面使用实际记录。

- **新建旅行**：点击“应用导入轨迹”，再为点位或路段填写正文并保存。已有旅行使用此操作会先确认，然后替换轨迹、清空正文与精选点位。
- **补齐已有旅行的时间和海拔**：保持原导入时间范围和定位误差，重新读取 CSV。只有各线路段及每个坐标完全一致时，才可点击“仅更新时间和海拔（保留正文）”。此操作只替换 `pointMeta`，保留轨迹、锚点、文字、图片与精选选择；仍需点击保存才写入数据库。坐标、点数或断段不一致时按钮禁用并说明原因，不自动猜测或移动正文锚点。

保存会将坐标及对应时间、海拔一起写入 Neon，封面统计、全程地图、随正文延伸的路线及点位元数据会自动使用已保存内容，不需要再手工填写统计或生成地图。CSV 不会自动生成游记文字、照片、正文点位或精选选择；这些内容在地图编辑区添加。

## 开发接口

Worker 文件：`src/workers/travel-csv.worker.ts`。编辑器为每次导入创建一个 Worker，并在取消或卸载时调用 `terminate()`。

输入：

```ts
{ file: File, options: { from: string, to: string, maxAccuracy: number, gapMinutes: number, gapKm: number } }
```

输出：

```ts
{ type: "progress", percent: number }
{ type: "complete", segments: [number, number][][], pointMeta: { time: number | null, altitude: number | null }[], stats: { rows, matched, points, segments, invalid, from, to } }
{ type: "error", error: string }
```

`src/lib/travel-csv.ts` 导出纯解析器及 `importTravelCsvFile`，可用 Node 24 直接运行测试：`node --test test/travel-csv.test.mjs`。浏览器页面只在 Worker 中调用文件导入，避免解析大文件时阻塞地图编辑。

已重新读取包含 714,194 条记录的原始足迹 CSV，按两次旅行的北京时间范围分别提取独立文件，未包含其他旅行记录：

| 旅行 | 提取范围（北京时间） | CSV 记录数 | 导入结果 |
| --- | --- | --- | --- |
| 西北环线2025 | `2025-09-29T11:44:33` 至 `2025-10-07T11:50:22` | 21,329 | 21,242 个点、17 段 |
| 伊犁2026 | `2026-06-24T08:02:36` 至 `2026-06-28T14:16:34` | 20,181 | 20,168 个点、8 段 |

两份文件使用定位误差上限 100 米及默认断段阈值导入，坐标和分段均与已有路线逐点完全一致。已在独立开发库的临时副本中，实际通过后台选取文件、读取、仅更新记录、保存、刷新回读及前台显示验证：原始时间和海拔逐项保留，14 / 9 处正文、锚点、图片及精选选择保持不变，封面统计与后续地图自动更新；验证后已清理临时记录。另以全新合成 CSV 验证从空草稿导入、添加点位和路段、编写 Markdown、保存发布及路线延伸，轨迹不一致时安全更新被禁用，原替换操作仍需确认。
